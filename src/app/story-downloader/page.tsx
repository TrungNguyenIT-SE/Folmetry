import { LocalizedRoutePage } from "@/components/pages/localized-route-page";

export const metadata = { title: "Public Stories and Highlights" };

export default function StoryDownloaderPage() {
  return <LocalizedRoutePage route="stories" />;
}
