# Synthetic Facebook export fixtures

These files are recreated, non-production compatibility fixtures. Current-schema coverage for `connections/friends/your_friends.json`, `people_who_followed_you.json` (`followers_v3`), and `who_you've_followed.json` (`following_v3`) lives in the adapter and worker tests. Names and timestamps are synthetic and do not identify real Folmetry users.
