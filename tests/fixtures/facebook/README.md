# Synthetic Facebook export fixtures

These files are recreated, non-production fixtures for the current Facebook JSON export family. They model `friends_and_followers/friends.json` (`friends_v2`) and optional `following.json` (`following_v2`). Names and timestamps are synthetic and do not identify real Folmetry users.
